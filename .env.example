# KIS backend — Supabase connection.
# Copy this file to `.env.local` and fill in the two values from your Supabase project
# (Project Settings → API). Leave them blank to keep running on the local demo store.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
