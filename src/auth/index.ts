/** Auth module barrel — Supabase-auth provider, hook, and the login screen. */
export { AuthProvider, useAuth } from './AuthProvider';
export type { AuthApi, AuthProfile } from './AuthProvider';
export { default as LoginScreen } from './LoginScreen';
